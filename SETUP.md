# دليل التركيب — نظام البلاغات وأوامر العمل

هذا الدليل يشرح ربط النموذج (`index.html`) بحساب Google شخصي، بحيث تُحفظ البيانات في جدول بيانات والصور في Drive.

**الوقت المتوقع:** ٢٠ دقيقة تقريباً
**تحتاج:** حساب Google شخصي (ليس بريد العمل)

---

## قبل أن تبدأ

الملفات الثلاثة في هذا المجلد:

| الملف | دوره |
|---|---|
| `index.html` | النموذج الذي يفتحه الفنيون |
| `Code.gs` | كود الخادم — يُلصق في Apps Script (لا يبقى على جهازك) |
| `SETUP.md` | هذا الدليل |

---

## الخطوة ١ — سجّل الدخول بالحساب الصحيح

افتح المتصفح وسجّل الدخول بحساب Google **الشخصي** الذي ستستضيف عليه النظام.

> ⚠️ **تأكد من هذا قبل أي خطوة.** كل ما يلي يُنشأ داخل الحساب الذي أنت مسجّل به الآن. لو كنت مسجّلاً بحساب آخر في نفس المتصفح، افتح **نافذة تصفّح متخفٍّ (Incognito)** وسجّل الدخول بالحساب المطلوب فيها، ونفّذ كل الخطوات هناك.

---

## الخطوة ٢ — أنشئ جدول البيانات وانسخ معرّفه

1. افتح [sheets.new](https://sheets.new) — يُنشأ جدول فارغ جديد.
2. سمِّه من الأعلى، مثلاً: `Incident Reports Database`
3. انظر إلى رابط الصفحة في المتصفح. سيكون بهذا الشكل:

```
https://docs.google.com/spreadsheets/d/1aBcD3fGhIjKlMnOpQrStUvWxYz1234567890/edit
                                      └──────────── هذا هو المعرّف ────────────┘
```

4. **انسخ الجزء الذي بين `/d/` و `/edit`** واحتفظ به. هذا هو `SPREADSHEET_ID`.

> لا تنشئ الشيتات يدوياً. السكربت ينشئ `WorkOrders` و `Inspections` بأعمدتها ورؤوسها تلقائياً.

---

## الخطوة ٣ — الصق كود الخادم

1. من نفس جدول البيانات: **الإضافات (Extensions) ← Apps Script**
2. تُفتح نافذة محرر فيها ملف `Code.gs` يحتوي على دالة فارغة.
3. **امسح كل محتواه** (Ctrl+A ثم Delete).
4. افتح ملف `Code.gs` من هذا المجلد، انسخ **كل** محتواه، والصقه مكانه.
5. في السطر ١٦ تقريباً ستجد:

```js
const SPREADSHEET_ID = "ضع_معرف_الشيت_هنا";
```

استبدل `ضع_معرف_الشيت_هنا` بالمعرّف الذي نسخته في الخطوة ٢ — **مع إبقاء علامتي التنصيص**:

```js
const SPREADSHEET_ID = "1aBcD3fGhIjKlMnOpQrStUvWxYz1234567890";
```

6. اضغط **حفظ** (أيقونة القرص، أو Ctrl+S).

---

## الخطوة ٤ — شغّل `setup` مرة واحدة ومنح الأذونات

1. من القائمة المنسدلة أعلى المحرر، اختر الدالة **`setup`**.
2. اضغط **تشغيل (Run)**.
3. ستظهر نافذة **"مطلوب التفويض (Authorization required)"** ← اضغط **مراجعة الأذونات**.
4. اختر حسابك.
5. ستظهر شاشة تحذير: **"لم يتحقّق Google من هذا التطبيق"**.

   > 🔵 **هذا طبيعي ومتوقّع.** التحذير يظهر لأي سكربت يكتبه شخص لنفسه ولم يمر بمراجعة Google التجارية. أنت مؤلف هذا الكود ومالكه.
   >
   > اضغط **خيارات متقدمة (Advanced)** ← ثم **الانتقال إلى … (غير آمن)** في الأسفل.

6. راجع الأذونات المطلوبة (الوصول لجداول بياناتك وملفات Drive) واضغط **سماح**.
7. في سجل التنفيذ بالأسفل يجب أن ترى:

```
✅ تم التجهيز بنجاح
```

8. ارجع لجدول البيانات — ستجد شيتين جديدين: **WorkOrders** و **Inspections** برؤوس زرقاء مثبّتة.

> 💡 **فعّل الفلتر مرة واحدة (اختياري لكنه مفيد):** في كل شيت، اضغط على خلية في صف الرأس ثم **البيانات ← إنشاء فلتر**. يتمدد تلقائياً مع كل صف جديد، ويتيح لك الفرز والتصفية (مثل: عرض كل الفحوصات `Fail` فقط).
>
> نُنشئه يدوياً لا برمجياً لأن الفلتر المُنشأ بالكود يثبُت على نطاق ثابت ولا يشمل الصفوف الجديدة.

> ❌ **لو ظهر خطأ "لم تضبط SPREADSHEET_ID بعد"** ← ارجع للخطوة ٣، لم يُحفظ التعديل.
> ❌ **لو ظهر خطأ يذكر `openById`** ← المعرّف الذي لصقته خاطئ. أعد نسخه من الرابط بدقة.

---

## الخطوة ٥ — انشر السكربت كتطبيق ويب

1. من أعلى يمين المحرر: **نشر (Deploy) ← عملية نشر جديدة (New deployment)**
2. اضغط أيقونة الترس ⚙️ بجانب "تحديد النوع" ← اختر **تطبيق ويب (Web app)**
3. اضبط الحقول **بالضبط** هكذا:

| الحقل | القيمة |
|---|---|
| الوصف (Description) | `v1` |
| التنفيذ باسم (Execute as) | **أنا (Me)** |
| من لديه حق الوصول (Who has access) | **أي شخص (Anyone)** |

> ⚠️ **"أي شخص (Anyone)" — وليس "أي شخص لديه حساب Google".**
> هذا هو ما يجعل الرابط يعمل للفنيين بلا تسجيل دخول. "التنفيذ باسم: أنا" هو ما يجعل السكربت يكتب في شيتك ودرايفك بصلاحياتك أنت.

4. اضغط **نشر (Deploy)**.
5. انسخ **رابط تطبيق الويب (Web app URL)** — ينتهي بـ `/exec`.

```
https://script.google.com/macros/s/AKfycb....../exec
```

---

## الخطوة ٦ — تحقّق أن الخادم يعمل

الصق رابط `/exec` في تبويب متصفح جديد واضغط Enter. يجب أن ترى:

```json
{"ok":true,"service":"Incident Report & Work Order System","message":"..."}
```

لو رأيت هذا، الخادم شغّال. لو رأيت صفحة خطأ، راجع الخطوة ٥.

---

## الخطوة ٧ — اربط النموذج بالخادم

1. افتح `index.html` بمحرر نصوص.
2. ابحث عن السطر (قرابة السطر ٣٥٣):

```js
const SCRIPT_URL = "ضع_رابط_النشر_هنا";
```

3. استبدله برابط `/exec` الذي نسخته:

```js
const SCRIPT_URL = "https://script.google.com/macros/s/AKfycb....../exec";
```

4. احفظ الملف.

---

## الخطوة ٨ — الاختبار

افتح `index.html` في المتصفح ونفّذ هذه الاختبارات بالترتيب:

| # | الاختبار | النتيجة المتوقعة |
|---|---|---|
| ١ | عبّئ بلاغاً كاملاً، حوّله لأمر عمل، ارفع ٣ صور، أرسل | رسالة خضراء + رقم البلاغ + عدد الفحوصات + رابط "📁 فتح مجلد الصور" |
| ٢ | افتح `WorkOrders` | صف واحد جديد، وروابط `Before` / `During` / `After` / `Open Folder` تُفتح جميعها |
| ٣ | افتح `Inspections` | صف مستقل لكل فحص، مرتبط برقم البلاغ نفسه |
| ٤ | أرسل بلاغاً **بلا صور** | ينجح، وخلايا الصور تبقى فارغة |
| ٥ | أرسل مع ترك "اسم العميل" فارغاً | رسالة حمراء تحدد الحقل الناقص، ولا يُكتب أي صف |
| ٦ | أرسل بالواجهة العربية | أسماء المعدات في `Inspections` تظهر **بالإنجليزية** (مقصود — لتوحيد البيانات) |
| ٧ | اقطع الإنترنت وأرسل | رسالة حمراء + **كل بياناتك وصورك تبقى في النموذج** + الزر يعود قابلاً للضغط |

---

## التصدير إلى Excel

من جدول البيانات: **ملف ← تنزيل ← Microsoft Excel (.xlsx)**

الشيتان ينزلان كورقتين في ملف واحد، وروابط الصور تبقى قابلة للنقر داخل Excel.

---

## حلّ المشكلات

### الإرسال يأخذ وقتاً طويلاً أو يظهر "إعادة المحاولة تلقائياً"

**هذا متوقّع ولا يحتاج تدخّلاً.** قياسات فعلية على هذا النظام:

| الحالة | المدة |
|---|---|
| بلا صور | ~١٠ ثوانٍ |
| بثلاث صور | **٩ إلى ٤٠ ثانية** (متذبذبة) |

Apps Script لا يرجّع النتيجة مباشرة؛ يحوّل إلى رابط بمفتاح قصير العمر، وGoogle يفشل أحياناً في جلبه فيرجّع `404`. **عطل متقطّع من طرف Google، لا علاقة له بالكود** — قيس عملياً أنه يصيب نحو ثلث الطلبات، بلا ارتباط ثابت بمدة التنفيذ.

لذلك تعيد الصفحة المحاولة تلقائياً حتى **٣ مرات**. وهذا آمن تماماً: الخادم يتحقق من `Incident No` قبل الكتابة، فلو نجحت المحاولة الأولى فعلاً وضاع ردّها، ترجع الثانية `duplicate` بلا كتابة صف مزدوج. **لا تغلق الصفحة أثناء المحاولات.**

### الرسالة: "تعذّر الاتصال بالخادم" (بعد الثلاث محاولات)

1. تأكد أن النشر على **"أي شخص (Anyone)"** وليس "أي شخص لديه حساب Google".

2. **افتح الصفحة عبر خادم محلي بدل `file://`** — يحل مشاكل CORS نهائياً:
   ```
   python -m http.server 8000
   ```
   ثم افتح `http://localhost:8000/index.html`

3. **الأهم: افتح الشيت قبل إعادة الإرسال.** قد تكون البيانات محفوظة فعلاً وضاع الرد فقط.

4. افتح **F12 ← Console** وانسخ نص الخطأ الأحمر.

### الرسالة: "لم يتم ضبط رابط الخادم بعد"

لم تُنفَّذ الخطوة ٧، أو الرابط الملصوق لا يحتوي `script.google.com`.

### الصور لا تُفتح لغير صاحب الحساب

السكربت يضبط المشاركة تلقائياً على "أي شخص لديه الرابط". لو حدث خلاف ذلك، تحقق أن سياسات حسابك لا تمنع المشاركة الخارجية.

### عدّلت `Code.gs` لكن لا شيء تغيّر ⚠️

**أشهر مشكلة في Apps Script.** الحفظ لا يكفي — الرابط يظل يخدم النسخة القديمة حتى تنشر نسخة جديدة:

**نشر ← إدارة عمليات النشر ← ✏️ (تعديل) ← الإصدار: نسخة جديدة (New version) ← نشر**

الرابط لا يتغير، ولا حاجة لتعديل `index.html`.

---

## ملاحظات

- **الشعار:** النموذج يستدعي `logo.jpg` من نفس المجلد. ضع الملف هناك ليظهر — وبدونه يُخفى تلقائياً بلا خطأ.
- **جودة الصور:** مضبوطة على `1280px / 0.75` في أعلى قسم السكربت في `index.html` (المتغيرات `PHOTO_MAX_WIDTH` و `PHOTO_QUALITY`). لو كان الإنترنت ضعيفاً في المواقع، خفّضها إلى `1024` و `0.70`.
- **السعة:** ~١٫٨ ميجابايت لكل بلاغ بثلاث صور. الحساب المجاني ١٥ جيجابايت ← تكفي قرابة ٨٠٠٠ بلاغ.
- **النسخ الاحتياطي:** فعّل التحقق بخطوتين على الحساب، ونزّل نسخة `.xlsx` دورياً — البيانات كلها تعيش في حساب واحد.
- **توزيع النموذج على الفنيين:** أبسط طريقة هي رفع `index.html` على استضافة مجانية (GitHub Pages مثلاً) ومشاركة الرابط. هذا أيضاً يحل مشكلة CORS المذكورة أعلاه نهائياً.
