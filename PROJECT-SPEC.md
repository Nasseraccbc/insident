# نظام إدارة التشغيل والصيانة — الهيئة السعودية للمهندسين

> **المناقصة:** SCE-2026-0119
> **النوع:** تطبيق ويب تفاعلي (SPA) لإدارة المنشآت والصيانة — CAFM / CMMS
> **آخر تحديث:** 2026-09-14
> **الحالة:** التصميم مُعتمد · التنفيذ لم يبدأ

---

## ١. الغاية

نظام واحد يحلّ محلّ نظامين منفصلين قائمين، ويجمع دورة العمل كاملة من استلام البلاغ إلى الاعتماد النهائي:

```
بلاغ / طلب  ←  توزيع مهمة  ←  تنفيذ ميداني وتوثيق  ←  اعتماد  ←  مؤشرات أداء
```

وتُخزَّن البيانات في قاعدة بيانات مشتركة حقيقية بدل تخزين محلي في المتصفح.

---

## ٢. ما هو قائم اليوم

| الأصل | الوصف | المصير |
|---|---|---|
| `index.html` | نموذج بلاغ من مرحلتين، ٣ صور، مؤقتات SLA. باك-إند Google Apps Script يكتب في شيتين ويرفع الصور إلى Drive — **مبنيّ ومُختبَر** | **يُفكَّك.** محتواه ينتقل إلى `WO-01` داخل النظام الجديد؛ الباك-إند والشيتات و Drive تُلغى بالكامل |
| `النماذج-التشغيلية-SCE-2026-0119_8 (2).html` | ٢٣٦٢ سطراً · ١٦ نموذجاً · ١٨٧ حقلاً · ٤٧ قسماً. **محرّك نماذج مدفوع بالبيانات** + مسار اعتماد ٦ مراحل + لوحة مؤشرات + ثنائي اللغة + وضع ليلي. يخزّن في `localStorage` فقط | **يصير الأساس.** يُحتفظ بالمحرّك والنماذج والمسار واللوحة؛ يُستبدل `localStorage` بـ Supabase |
| `text.md` | مستند المتطلبات الأصلي | مرجع |

### لماذا محرّك النماذج هو الأساس

في ملف النماذج التشغيلية، النموذج **بيانات لا كود**:

```js
{code:"HR-01", name:"جدول إجازات الكادر", secs:[
  {t:"بيانات الخطة", f:[
    {k:"no",   l:"رقم الخطة",      t:"text"},
    {k:"rule", l:"قاعدة التداخل",  t:"sel", o:["يومان","ثلاثة أيام"]}
  ]}
]}
```

ومعه مُصيِّر عام واحد يرسم النماذج الستة عشر جميعاً، ويدعم عشرة أنواع حقول: `text` · `area` · `sel` · `date` · `time` · `num` · `rows` (جداول متعددة الأعمدة) · `chkg` · `reg` · `calc` — إضافة إلى مخططات Gantt ومصفوفة مخاطر ومؤقتات وتواقيع.

أما `index.html` فنموذج واحد مكتوب يدوياً في HTML، كل حقل سطر ثابت.

> **النتيجة:** إضافة النموذج العشرين لاحقاً = إضافة سطر بيانات، بلا كود جديد. البديل — صفحة مستقلة لكل نموذج — يعني تسع عشرة صفحة، وأي تعديل على مسار الاعتماد يتكرر تسع عشرة مرة.

---

## ٣. القرارات المعمارية

| القرار | البديل المرفوض | السبب |
|---|---|---|
| **Supabase** (Postgres + Auth + Storage) | Google Sheets · localStorage · OneDrive | يحلّ الدخول والصلاحيات والتخزين المشترك والصور في منصّة واحدة. القرار انقلب عن اختيار Google السابق لأن المتطلب تغيّر من "رابط مفتوح بلا تسجيل دخول" إلى "أدوار وصلاحيات" |
| **محرّك نماذج + ودجات خاصة** | صفحة لكل نموذج | أعلاه |
| **جدول سجلات واحد بعمود JSONB** | جدول لكل نموذج (١٩ جدولاً) | حقول `HR-01` لا تشبه `HSE-03`؛ جدول لكل حقل يعني ١٨٧ عموداً. الحقول التي تُفلتر وتُحسب عليها اللوحة تُرقّى إلى أعمدة حقيقية |
| **منطق الأعمال في القاعدة** (Triggers / Functions) | منطق في المتصفح | توليد أمر العمل التصحيحي يجب أن يقع حتى لو أُغلقت الصفحة |
| **التنازل عن شرط "ملف HTML واحد مستقل"** | التمسّك بالبرومبت حرفياً | الشرط مكتوب لعرض تجريبي؛ المطلوب نظام إنتاجي ببيانات مشتركة وصلاحيات. تعذّر الجمع بينهما |

---

## ٤. الأدوار والصلاحيات

أربع لوحات مختلفة، لا مجرد مستويات وصول. **المستخدمون يُنشَأون يدوياً — لا تسجيل ذاتي.**

| الدور | مَن | الصلاحيات |
|---|---|---|
| **الإدارة العليا** | **فيصل** — مدير المشروع | كل السجلات · لوحة المؤشرات كاملة · **الاعتماد التشغيلي** |
| **الامتثال** | **خلف** — ممثل الهيئة | لوحة المؤشرات · المخاطر والحوادث · **الاعتماد النهائي** |
| **المشرف التشغيلي** | **عمّار** | استقبال البلاغات وطلبات الضيافة والقاعات وتصنيفها · **إنشاء المهام وتوزيعها** · متابعة مؤقتات SLA |
| **التنفيذ الميداني** | الفنيون | **مهامه هو فقط** · تعبئة النموذج · رفع صور قبل/أثناء/بعد · التوقيع · تغيير الحالة |
| **الخدمة الذاتية** | أي موظف | تقديم طلب ضيافة أو تجهيز قاعة فقط |

**الدفعة الأولى: أربعة مستخدمين** — فيصل، خلف، عمّار، وفنيّ واحد. الباقي يُضاف لاحقاً.

---

## ٥. توزيع المهام

**تصحيح جوهري:** البرومبت الأصلي كتب «Auto-Dispatching»، والمطلوب فعلياً **اختيار يدوي** — عمّار يقرّر، والقائمة مرتّبة بالتخصص لتسهيل الاختيار:

```
اختر المكلَّف:
  ▸ كهرباء   →  الفني الأول · الفني الثاني
  ▸ سباكة    →  الفني الأول · الفني الثاني
  ▸ تكييف    →  …
```

### الدورة الكاملة

```
عمّار ينشئ مهمة  →  يختار الفني من قائمة مجمّعة بالتخصص
       ↓
الفني يفتح لوحته  →  يرى مهامه هو فقط
       ↓
يعبّئ النموذج + صور + توقيع  →  يغيّر الحالة
       ↓
السجل يصبّ في لوحة المؤشرات  →  بطاقات ورسوم بيانية
       ↓
فيصل يعتمد تشغيلياً  →  خلف يعتمد نهائياً
```

---

## ٦. النماذج

### ١٦ نموذجاً جاهزاً (تُنقل كما هي)

| الملحق | الرموز |
|---|---|
| **HR** | `HR-01` إجازات وبدلاء · `HR-02` استبدال طارئ |
| **QA** | `QA-01` جودة التنفيذ · `QA-02` جودة الصيانة · `QA-03` النظافة والضيافة · `QA-04` متابعة المخالفات · `QA-05` التقرير الدوري |
| **HSE** | `HSE-01` تصريح عمل · `HSE-02` صحة وسلامة · `HSE-03` الحوادث والتحقيق · `HSE-04` التدريب · `HSE-05` صحائف SDS |
| **RSK** | `RSK-01` سجل المخاطر — مصفوفة الاحتمال × الأثر |
| **WO** | `WO-01` · `WO-02` · `WO-03` أوامر العمل |

### ٣ نماذج جديدة — تحتاج ودجات مخصّصة

هذه لا تُختزل إلى حقول عادية؛ تُسجَّل **أنواع حقول جديدة** في المحرّك نفسه (`t:"drinks"`, `t:"triparty"`, `t:"egrid"`) فيتوسّع المحرّك ولا ينكسر.

#### `HS-01` — طلب ضيافة للمكاتب
شبكة **١٤ مشروباً** بكمية وخيارات لكل صنف:

| الفئة | الأصناف |
|---|---|
| قهوة | إسبريسو · أمريكانو · كابوتشينو · لاتيه · فلات وايت · موكا |
| ساخنة | شاي أحمر · أخضر · نعناع · ليمون وزنجبيل · هوت شوكليت |
| باردة | مياه معدنية · مياه فوارة · ليمون بنعناع |

إضافة إلى: أرقام التتبّع (`Order No.` · `CU No.` · `CO No.`) · مراحل التوقيت · صور قبل/أثناء/بعد · ملاحظات التحضير (نسبة السكر، نوع الحليب) · توقيع المستلم.

#### `MR-01` — تجهيز قاعات الاجتماعات
قائمة تحقّق ثلاثية باعتماد ثلاثي مستقل:

| الجهة | البنود |
|---|---|
| **الضيافة** | ترتيب القاعة · تجهيز الأدوات · عبوات المياه · المشروبات · المستلزمات · تحقّق نهائي |
| **النظافة** | الأسطح والطاولات والأثاث · الأرضيات والسجاد والحاويات |
| **الإدارة** | جاهزية الطاقم · الجاهزية الفنية (تكييف · شاشات · إضاءة) |

الاعتماد: رئيس الضيافة ← رئيس النظافة ← مدير الموقع (بتاريخ ووقت).

#### `QA-02-EL` — الفحص الشامل للأصول الكهربائية
**٢٤ أصلاً × ١٢ نقطة فحص = ٢٨٨ خانة تفاعلية.**

*الأصول:* MDB · لوحات فرعية · قواطع · كوابل · تأريض · إضاءة ومفاتيح · UPS · مولدات · ATS · Busbars · لوحات تكييف · محوّلات · لوحات مضخات · بطاريات وشواحن · إضاءة طوارئ · إنذار حريق · لوحات مصاعد · مكثفات تحسين معامل القدرة · عوازل · مقابس · مانعات صواعق · لوحات تهوية · عدادات طاقة · أجهزة مراقبة اللوحات.

*نقاط الفحص:* ظاهري · نظافة · توصيل كهربائي · عزل · قياس تيار واتزان الأوجه · تسريب (ELCB) · تأريض · أداء وظيفي · قطع غيار · حرارة (Infrared) · تآكل · تلف فيزيائي.

> ### ⚠️ أخطر منطق في النظام
> وضع **×** على أي نقطة فحص ← يُظلَّل الأصل أحمر ← **يُولَّد أمر عمل تصحيحي عاجل تلقائياً ويُوجَّه لفني الكهرباء.**
>
> هذا **منطق أعمال يعبر بين نموذجين ويكتب في جدول آخر**، لا ودجة واجهة. مكانه الصحيح **داخل القاعدة** (Trigger / Function) لا في المتصفح — ليعمل حتى لو أُغلقت الصفحة.

---

## ٧. اتفاقية مستوى الخدمة (SLA)

**ثلاثة مستويات فقط.** يُحذف المستوى الرابع "منخفض" الموجود في `index.html` الحالي.

| الأولوية | الاستجابة | الإغلاق |
|---|---|---|
| **حرج** | ١٥ دقيقة | ساعتان |
| **عالي** | ٣٠ دقيقة | ٨ ساعات |
| **متوسط** | ساعتان | ٢٤ ساعة |

المؤقّت يُحتسب من طابع زمني على الخادم لا من ساعة المتصفح، ويُلوَّن أخضر ضمن المهلة وأحمر عند التجاوز.

---

## ٨. مسار الاعتماد

ست مراحل، مع سجل تاريخي لكل انتقال (مَن · متى · ملاحظة):

```
مسودة → مُرسل لمشرف الموقع → قيد مراجعة مدير المشروع
      → معتمد داخلياً → مرفوع لممثل الهيئة → مغلق ومعتمد
```

مع إمكانية **الإرجاع للتصحيح** من أي مرحلة.

---

## ٩. لوحة المؤشرات

**بطاقات:** إجمالي أوامر العمل · المفتوحة · المغلقة · نسبة الالتزام بـ SLA · متوسط زمن الاستجابة · المخاطر المفتوحة · المخاطر الحرجة · بانتظار الاعتماد · الحوادث · طلبات الضيافة اليومية.

**رسوم بيانية:** الالتزام بـ SLA عبر أيام الأسبوع · منحنى ١٤ يوماً · خريطة حرارية للمخاطر (٥×٥) · توزّع السجلات على النماذج · توزّع الحالات.

**جداول:** المعلّقات بانتظار الاعتماد · أوامر العمل الحيّة بمؤقتاتها · سجل اعتمادات فيصل وخلف.

---

## ١٠. نموذج البيانات

```
profiles         المستخدمون: الاسم · الدور · التخصص · الحالة
records          سجل لكل نموذج معبّأ
                 ├─ أعمدة حقيقية (تُفلتر وتُحسب عليها اللوحة):
                 │    form_code · state · priority · assigned_to
                 │    sla_response_at · sla_close_at
                 │    created_by · created_at · closed_at
                 └─ data (JSONB): باقي الحقول مهما اختلفت بين النماذج
record_history   كل انتقال في مسار الاعتماد الست
tasks            المهام التي ينشئها عمّار ويوزّعها
attachments      الصور — الملفات في Supabase Storage والبيانات الوصفية هنا
assets           الـ٢٤ أصلاً الكهربائية
```

**مبدأ التقسيم:** ما تحتاجه اللوحة للفلترة والحساب ← عمود حقيقي مفهرس. ما يختلف بين نموذج وآخر ← داخل `JSONB`.

---

## ١١. الأمان

### سياسات RLS

**قاعدة ملزمة: كل جدول عليه RLS مُفعَّل وسياسات صريحة.** جدول عليه RLS بلا سياسات = ممنوع على الجميع، وهذا هو السلوك الافتراضي المطلوب.

```sql
-- الفني يرى مهامه هو فقط
create policy "technician sees own records"
  on records for select
  using (assigned_to = auth.uid());
```

**إعدادات Supabase المعتمدة:** `Enable Data API` ✅ · `Enable automatic RLS` ✅ **(لا يُطفأ إطلاقاً)** · `Automatically expose new tables` ✅ (آمن ما دام RLS التلقائي مفعّلاً).

### المفاتيح

| المفتاح | الطبيعة | الموضع |
|---|---|---|
| `SUPABASE_URL` · `anon key` | عامّ وآمن للنشر — الحماية من RLS لا من الإخفاء | داخل كود الصفحة · يُرفع Git |
| `service_role` | 🔴 يتجاوز RLS كاملاً | **لا يُستخدم في هذا المشروع** |
| Personal Access Token | 🔴 على مستوى الحساب — يقرأ ويحذف كل المشاريع | `.env` فقط · محجوب عن Git · **يُلغى بعد بناء القاعدة** |

**قواعد ثابتة:** لا يُلصق أي سرّ في محادثة · `.env` محجوب ومتحقَّق منه · لا تنفيذ `DROP` أو `DELETE` أو `TRUNCATE` بلا موافقة صريحة.

---

## ١٢. المتطلبات التقنية

| البند | القرار |
|---|---|
| **اللغة** | **عربي + إنجليزي** مع زر تبديل — المحرّك يدعمها أصلاً (RTL / LTR) |
| **الهوية** | أخضر غامق `#1a4d2e` · ذهبي `#d4af37` — تحلّ محلّ الأزرق `#1e4268` الحالي |
| **المكتبات** | Chart.js · FontAwesome · supabase-js — لا غير |
| **التفاعلية** | تغيير الحالات والتوزيع ديناميكياً في DOM بلا إعادة تحميل |
| **الصور** | Supabase Storage · ضغط قبل الرفع ١٢٨٠px / جودة 0.75 |
| **الاستضافة** | Vercel · فريق `insident` · مستودع `Nasseraccbc/insident` |

---

## ١٣. مراحل التنفيذ

| # | المرحلة | المخرج |
|---|---|---|
| **١** | القاعدة والمصادقة | جداول · سياسات RLS · Storage · المستخدمون الأربعة |
| **٢** | الهيكل والأدوار | تسجيل دخول · توجيه حسب الدور · أربع لوحات فارغة |
| **٣** | المحرّك والنماذج الـ١٦ | نقل المحرّك من `localStorage` إلى Supabase |
| **٤** | `WO-01` وSLA | نقل نموذج البلاغ · المؤقتات · الصور |
| **٥** | لوحة عمّار | إنشاء المهام · التوزيع بالتخصص |
| **٦** | النماذج الثلاثة الجديدة | `HS-01` · `MR-01` · `QA-02-EL` + مشغّل أمر العمل التصحيحي |
| **٧** | لوحة المؤشرات | البطاقات والرسوم والجداول |
| **٨** | الاعتماد والنشر | مسار فيصل وخلف · النشر على Vercel |

كل مرحلة تُسلَّم عاملة ومُختبَرة قبل التالية.

---

## ١٤. بنود معلّقة

- [ ] **الفني الرابع** — الاسم والتخصص
- [ ] **قائمة التخصصات النهائية** — المقترح: كهرباء · سباكة · تكييف · ضيافة · نظافة
- [ ] **`SUPABASE_URL`** — بعد إنشاء المشروع
- [ ] **طريقة بناء القاعدة** — دخول مباشر عبر Management API (توكن في `.env`) أم ملفات SQL تُلصق يدوياً
- [ ] **رفع المشروع إلى GitHub** — إذن `git push` أُضيف في `.claude/settings.json` لكنه يسري في جلسة جديدة فقط

---

## ملحق — البرومبت الجاهز

> نصّ مكتفٍ بذاته لتكليف مهندس أو وكيل ذكاء اصطناعي بهذا المشروع.

```
أنت مهندس برمجيات خبير في أنظمة إدارة المنشآت والصيانة (CAFM / CMMS).

ابنِ تطبيق ويب من صفحة واحدة (SPA) لإدارة التشغيل والصيانة للهيئة السعودية
للمهندسين — مناقصة SCE-2026-0119 — ثنائي اللغة (عربي RTL افتراضاً / إنجليزي)،
بهوية خضراء ذهبية (#1a4d2e / #d4af37).

المعمارية:
• محرّك نماذج مدفوع بالبيانات: النموذج تعريف JSON لا كود. مُصيِّر عام واحد
  يدعم: text · area · sel · date · time · num · rows · chkg · reg · calc،
  وثلاثة أنواع مخصّصة: drinks (١٤ مشروباً) · triparty (تحقّق ثلاثي)
  · egrid (٢٤ أصلاً × ١٢ فحصاً).
• Supabase: Postgres + Auth + Storage. جدول سجلات واحد بأعمدة حقيقية للفلترة
  (form_code · state · priority · assigned_to · مواعيد SLA) وعمود JSONB
  لبقية الحقول. RLS مفعّل على كل جدول بسياسات صريحة.
• منطق الأعمال العابر بين النماذج داخل القاعدة (Triggers / Functions) لا في
  المتصفح.

الأدوار: مدير المشروع (اعتماد تشغيلي) · ممثل الهيئة (اعتماد نهائي) · مشرف
تشغيلي (ينشئ المهام ويوزّعها يدوياً من قائمة مجمّعة بالتخصص) · فنيّ ميداني
(يرى مهامه هو فقط) · موظف (خدمة ذاتية). المستخدمون يُنشَأون يدوياً.

النماذج: ١٦ نموذجاً قائماً (HR · QA · HSE · RSK · WO) + ثلاثة جديدة:
HS-01 ضيافة · MR-01 قاعات · QA-02-EL فحص كهربائي.

SLA ثلاثة مستويات: حرج ١٥د/ساعتان · عالي ٣٠د/٨س · متوسط ساعتان/٢٤س،
محتسبة من وقت الخادم، بمؤقّت ملوّن.

مسار الاعتماد ست مراحل مع سجل تاريخي وإمكانية الإرجاع:
مسودة → مُرسل للمشرف → مراجعة مدير المشروع → معتمد داخلياً → مرفوع للهيئة
→ مغلق ومعتمد.

منطق حرج: وضع × على أي نقطة فحص في QA-02-EL يُظلِّل الأصل أحمر ويولّد أمر
عمل تصحيحياً عاجلاً موجّهاً لفني الكهرباء — عبر Trigger في القاعدة.

لوحة المؤشرات: بطاقات (أوامر العمل · الالتزام بـ SLA · متوسط الاستجابة
· المخاطر · المعلّقات · الحوادث) · رسوم Chart.js (الالتزام أسبوعياً · منحنى
١٤ يوماً · خريطة مخاطر ٥×٥) · جداول المعلّقات والأوامر الحيّة.

المكتبات: Chart.js · FontAwesome · supabase-js فقط.
```
